# Текст для коммита в Git

```
fix(vmix): Исправлена передача цветов команд и добавлена полная интеграция с vMix

Основные изменения:

- Исправлена критическая ошибка передачи цветов в vMix
  * Теперь используется правильная команда SetColor вместо SetText для изменения цвета полей
  * Добавлен метод setColor() в vmix-client.js для отправки команды SetColor
  * Обновлен метод updateInputFields() для поддержки отдельного параметра colorFields
  * Обновлена функция formatCurrentScoreData() для разделения полей цвета от текстовых полей
  * Цвета команд (match.teamA.color и match.teamB.color) автоматически передаются в vMix

- Реализована полная система типов полей для vMix
  * Текстовые поля (type: "text"): команда SetText
  * Поля цвета (type: "color"): команда SetColor
  * Поля видимости (type: "visibility"): команды SetTextVisibleOn/Off
  * Автоматическое разделение полей по типам при отправке команд

- Переработана страница настроек vMix
  * Новая структура интерфейса: вертикальные вкладки инпутов слева, панель редактирования справа
  * Каждый инпут настраивается индивидуально с возможностью включения/выключения
  * Детальная настройка полей для каждого инпута с поддержкой fieldIdentifier
  * Компоненты InputTabsList, InputEditPanel, FieldsList

- Реализована синхронизация данных с vMix в реальном времени
  * Автоматическое обновление инпута текущего счета при изменении данных матча
  * Debounce механизм для оптимизации частоты обновлений
  * Управление оверлеями с отслеживанием состояния через vMix API
  * Автоматическое разрешение имен инпутов в номера

- Расширен функционал vMix клиента
  * Метод updateInputField() для обновления текстовых полей
  * Метод setTextVisibility() для управления видимостью
  * Метод getOverlayState() для получения состояния оверлеев
  * Метод getInputs() для получения списка инпутов
  * Метод findInputNumberByIdentifier() для разрешения имен инпутов

Технические детали:
- Обновлены IPC handlers для поддержки colorFields в update-input-fields
- Обновлена конфигурация полей по умолчанию в vmix-input-configs.js
- Обновлена документация: CHANGELOG.md, vmix-api-reference.md, vmix-settings-redesign-plan.md
- Все этапы переработки страницы настроек отмечены как выполненные

Исправленные файлы:
- src/main/vmix-client.js - добавлен метод setColor(), обновлен updateInputFields()
- src/renderer/hooks/useVMix.js - обновлена логика форматирования данных с разделением по типам полей
- src/main/main.js - обновлен IPC handler для поддержки colorFields
- src/main/preload.js - обновлен API для передачи colorFields
- CHANGELOG.md - добавлены все изменения
- docs/vmix-api-reference.md - исправлена документация по команде SetColor
- docs/vmix-settings-redesign-plan.md - обновлен статус реализации

Breaking changes: Нет. Изменения обратно совместимы.
```
