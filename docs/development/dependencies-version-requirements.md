# Требования к версиям зависимостей

## ⚠️ Важно

Этот документ описывает **критические требования к версиям библиотек**, которые должны соблюдаться для корректной работы проекта. Нарушение этих требований может привести к ошибкам сборки и работы приложения.

## 📋 Требуемые версии

### React и связанные библиотеки

**React 18.3.1** (НЕ использовать React 19.x)
- `react`: `^18.3.1`
- `react-dom`: `^18.3.1`
- `@types/react`: `^18.3.12`
- `@types/react-dom`: `^18.3.1`

**Причина:** React 19 несовместим с React Router 6 и вызывает ошибки при сборке Vite (`createContext is not exported`).

**React Router 6.28.0** (НЕ использовать React Router 7.x)
- `react-router-dom`: `^6.28.0`

**Причина:** React Router 7 имеет проблемы совместимости с Vite при production сборке. React Router 6.28.0 - стабильная версия, хорошо протестированная и совместимая с React 18.

### Сборка и инструменты разработки

**Vite 7.3.x**
- `vite`: `^7.3.0`
- `@vitejs/plugin-react`: `^5.1.2`

**Причина:** Требуется для корректной обработки CommonJS модулей React и React Router в production сборке.

### Electron

**Electron 39.2.7**
- `electron`: `^39.2.7`
- `electron-builder`: `^26.0.12`

**Причина:** Стабильная версия, совместимая с текущей архитектурой проекта.

## 🔧 Конфигурация Vite

Для корректной работы с указанными версиями библиотек в `vite.config.js` должны быть следующие настройки:

```javascript
build: {
  commonjsOptions: {
    // Включаем обработку CommonJS модулей, включая React, React Router и их зависимости
    include: [
      /src\/shared\/.*/,
      /node_modules\/react/,
      /node_modules\/react-dom/,
      /node_modules\/react-router/,
      /node_modules\/react-router-dom/,
      /node_modules\/scheduler/,  // Важно: scheduler - зависимость React
    ],
    transformMixedEsModules: true,
    strictRequires: true,
    requireReturnsDefault: 'auto',
  },
}
```

## ❌ Известные проблемы несовместимости

### React 19 + React Router 6
**Проблема:** React 19 изменяет способ экспорта функций (`createContext`, `useContext` и т.д.), что приводит к ошибкам при сборке:
```
"createContext" is not exported by "node_modules/react/index.js"
```

**Решение:** Использовать React 18.3.1

### React Router 7 + Vite Production Build
**Проблема:** React Router 7 использует development chunks, которые не работают в production сборке Vite:
```
node_modules/react-router/dist/development/chunk-*.mjs: "createContext" is not exported
```

**Решение:** Использовать React Router 6.28.0

## 📝 Процедура обновления зависимостей

При необходимости обновить зависимости:

1. **Проверьте совместимость** новых версий с текущими требованиями
2. **Обновите этот документ**, если версии изменены
3. **Протестируйте сборку**: `npm run build` и `npm run build:electron`
4. **Проверьте работу приложения** в production режиме
5. **Убедитесь, что нет `require()` в bundle**: `Select-String -Path "dist\assets\*.js" -Pattern "require\("`

## 🔍 Проверка версий

Для проверки установленных версий:

```bash
npm list react react-dom react-router-dom
```

Для проверки отсутствия `require` в bundle:

```bash
Select-String -Path "dist\assets\*.js" -Pattern "\brequire\s*\("
```

Должно вернуть `0` совпадений.

## 📚 Дополнительная информация

- [React 18 Release Notes](https://react.dev/blog/2022/03/29/react-v18)
- [React Router 6 Documentation](https://reactrouter.com/en/main)
- [Vite Configuration](https://vitejs.dev/config/)
- [Electron Documentation](https://www.electronjs.org/docs)

---

**Последнее обновление:** 2025-01-27  
**Актуальные версии:** См. `package.json` в корне проекта
