# Отчёт о полном аудите безопасности VolleyScore Master

**Дата проведения аудита:** 2026-02-05  
**Версия приложения:** 1.0.8  
**Предыдущие аудиты:** QA_AUDIT_REPORT.md (2026-01-21), XSS_FIX_SUMMARY.md (2024), vmix-connection-audit-report.md (2025-01-27)

---

## Оглавление

1. [Резюме и общая оценка](#резюме-и-общая-оценка)
2. [Перечень уязвимостей](#перечень-уязвимостей)
3. [Легаси и дублирующий код](#легаси-и-дублирующий-код)
4. [Неиспользуемый код](#неиспользуемый-код)
5. [План решения проблем](#план-решения-проблем)

---

## Резюме и общая оценка

### Контекст предыдущих аудитов

| Документ | Фокус | Статус |
|----------|-------|--------|
| **QA_AUDIT_REPORT.md** | Качество кода, безопасность, производительность | Требует актуализации |
| **XSS_FIX_SUMMARY.md** | XSS в мобильном сервере | ✅ Исправлено (domUtils) |
| **vmix-connection-audit-report.md** | Обработка недоступности vMix | ✅ Пройден |
| **obfuscation-and-protection.md** | Защита от декомпиляции | Рекомендации |

### Текущая конфигурация безопасности Electron

- **nodeIntegration:** `false` ✅
- **contextIsolation:** `true` ✅
- **sandbox:** `false` (может потребоваться для доступа к Node.js)
- **preload.cjs:** Явный whitelist API через `contextBridge.exposeInMainWorld` ✅

---

## Перечень уязвимостей

### 🔴 Критические

#### 1. Path Traversal при загрузке логотипов

**Файл:** `src/main/logoManager.ts`  
**Функции:** `loadLogoFromFile()`, `cleanupLogosDirectory()` (если path из match)

**Описание:** При открытии матча с malicious JSON, поле `logoPath` может содержать path traversal: `"logos/../../../etc/passwd"`. Функция `path.join(logosDir, fileName)` разрешает `../` и может привести к чтению произвольных файлов.

**Код:**
```typescript
// logoManager.ts:131-135
if (logoPath.startsWith('logos/')) {
  const logosDir = getLogosDir();
  const fileName = logoPath.replace('logos/', '');  // "logos/../../../etc/passwd" -> "../../../etc/passwd"
  filePath = path.join(logosDir, fileName);         // Результат может выйти за пределы logosDir!
}
```

**Решение:** Проверять, что итоговый `path.resolve(logosDir, fileName)` остаётся внутри `logosDir`:
```typescript
const resolvedPath = path.resolve(logosDir, fileName);
if (!resolvedPath.startsWith(path.resolve(logosDir))) {
  throw new Error('Недопустимый путь к файлу');
}
```

---

#### 2. Отсутствие ограничения размера JSON в Express

**Файл:** `src/main/server.ts`  
**Строка:** `this.app.use(express.json());`

**Описание:** `express.json()` по умолчанию не ограничивает размер тела запроса. Злоумышленник может отправить очень большой JSON и вызвать DoS (исчерпание памяти).

**Решение:**
```javascript
this.app.use(express.json({ limit: '100kb' }));
this.app.use(express.urlencoded({ extended: true, limit: '100kb' }));
```

---

### 🟠 Средние

#### 3. Отсутствие rate limiting для мобильного API

**Файл:** `src/main/server.ts`  
**Статус:** Не реализовано (указано в QA_AUDIT_REPORT)

**Описание:** API мобильного сервера не защищён от brute-force и DoS через множественные запросы.

**Решение:** Добавить `express-rate-limit`:
```javascript
import rateLimit from 'express-rate-limit';
const apiLimiter = rateLimit({ windowMs: 60000, max: 100 });
this.app.use('/api/', apiLimiter);
```

---

#### 4. Отсутствие проверки размера файлов

**Файлы:** `src/main/fileManager.ts`, `src/main/logoManager.ts`

**Описание:**
- При сохранении матча нет проверки размера JSON (большой match может вызвать OOM)
- При загрузке логотипа из base64 нет лимита размера (DoS через огромный base64)

**Рекомендуемые лимиты:**
- Матч JSON: 5 MB
- Логотип base64: 2 MB

---

#### 5. XSS в Markdown (documentation-viewer)

**Файл:** `src/main/documentation-viewer.ts`  
**Строка:** `marked.parse(markdownContent)`

**Описание:** Библиотека `marked` по умолчанию не санитизирует HTML. Документация читается из локальных файлов (`docs/`), но если пользователь сможет модифицировать файлы или в будущем добавится загрузка внешнего markdown — возможен XSS.

**Риск:** Низкий (документация — доверенные локальные файлы). Рекомендуется санитизация для defense in depth.

**Решение:** Использовать `marked` с опцией `sanitize` или дополнительно прогонять HTML через DOMPurify/аналогичную библиотеку.

---

#### 6. CORS: Access-Control-Allow-Origin: '*'

**Файл:** `src/main/server.ts`  
**Строка:** `res.header('Access-Control-Allow-Origin', '*');`

**Описание:** Мобильный сервер разрешает запросы с любого origin. Для локальной сети это обычно допустимо, но увеличивает поверхность атаки при скомпрометированной сети.

**Рекомендация:** Ограничить origin при возможности (например, только `null` для локальных файлов и localhost).

---

#### 7. Отсутствие HTTPS для мобильного сервера

**Файл:** `src/main/server.ts`

**Описание:** Мобильный сервер работает только по HTTP. Данные передаются в открытом виде в локальной сети.

**Риск:** Средний. Для LAN-приложения приемлемо, но стоит добавить предупреждение в UI о незащищённом соединении.

---

### 🟡 Низкие

#### 8. Отсутствие Content Security Policy (CSP)

**Файлы:** `src/main/server.ts`, `src/main/main.ts`

**Описание:** CSP не настроен ни для мобильного сервера, ни для Electron renderer.

**Рекомендация:** Добавить заголовки CSP для мобильного сервера и настроить CSP для WebContents.

---

#### 9. Валидация sessionId

**Файл:** `src/main/server/api/MatchApiRoutes.ts`

**Описание:** `sessionId` из URL проверяется через `validateSession()`, но формат UUID не валидируется строго. Теоретически возможны edge cases при нестандартных значениях.

**Рекомендация:** Проверять формат UUID перед использованием.

---

### ✅ Исправлено ранее

| Уязвимость | Документ | Статус |
|------------|----------|--------|
| XSS в мобильном сервере (innerHTML) | XSS_FIX_SUMMARY.md | ✅ Исправлено (domUtils.ts) |
| Утечка данных при недоступности vMix | vmix-connection-audit-report.md | ✅ Корректная обработка |

---

## Легаси и дублирующий код

### 1. Дублирование useMatch: useMatch.js и useMatch.ts

**Файлы:** `src/renderer/hooks/useMatch.js`, `src/renderer/hooks/useMatch.ts`

**Описание:** Существуют две реализации хука `useMatch`:
- `useMatch.js` — используется в `MatchControlPage.jsx` и тестах
- `useMatch.ts` — TypeScript-версия с типизацией, по сути дублирует логику

**Риск:** Расхождение реализаций при изменениях, путаница при поддержке.

**Рекомендация:** Оставить одну версию. Варианты:
- Удалить `useMatch.js`, перевести все импорты на `useMatch.ts`
- Или удалить `useMatch.ts` и постепенно мигрировать `useMatch.js` на TypeScript

---

### 2. Смешанные расширения импортов (.js / .ts)

**Описание:** В TypeScript-файлах импорты указывают `.js` для модулей без скомпилированного вывода (например, `volleyballRules.js`). Это создаёт риск путаницы при рефакторинге.

---

### 3. Использование `@ts-ignore` и `any`

**Файлы:** `SetService.ts`, `HistoryService.ts`, `MatchApiController.ts` и др.

**Описание:** В проекте используются `@ts-ignore` и `any` для обхода типизации при работе с JS-модулями. Снижает пользу TypeScript и маскирует потенциальные ошибки.

---

## Неиспользуемый код

### 1. useMatch.ts (если не используется)

**Проверка:** `MatchControlPage.jsx` импортирует `useMatch.js`. `useMatch.ts` не импортируется напрямую в приложении. Тесты используют `useMatch.js`.

**Вывод:** `useMatch.ts` — дубликат; либо неиспользуемый, либо используется через resolution (нужно проверить конфигурацию Vite/резолвинг).

---

### 2. Рекомендуемая проверка

Запустить:
```bash
npm run lint:unused
```

И статический анализ (например, `ts-prune` или `knip`) для поиска неиспользуемых экспортов.

---

## План решения проблем

### Фаза 1: Критические (1–2 дня)

| № | Задача | Файлы | Оценка |
|---|--------|-------|--------|
| 1 | Исправить path traversal в logoManager | `logoManager.ts` | 2–4 ч |
| 2 | Добавить limit для express.json/urlencoded | `server.ts` | 0.5 ч |
| 3 | Добавить проверку размера match JSON при сохранении | `fileManager.ts` | 1–2 ч |
| 4 | Добавить проверку размера base64 логотипа | `logoManager.ts` | 1 ч |

---

### Фаза 2: Средние (3–5 дней)

| № | Задача | Файлы | Оценка |
|---|--------|-------|--------|
| 5 | Добавить rate limiting для API | `server.ts`, новый `middleware/rateLimit.ts` | 2–4 ч |
| 6 | Устранить дублирование useMatch (оставить одну версию) | `useMatch.js`, `useMatch.ts`, `MatchControlPage.jsx`, тесты | 4–8 ч |
| 7 | (Опционально) Санитизация HTML в documentation-viewer | `documentation-viewer.ts` | 1–2 ч |
| 8 | Ужесточить валидацию sessionId (UUID) | `MatchApiRoutes.ts`, `server.ts` | 1 ч |

---

### Фаза 3: Низкие и улучшения (по мере возможности)

| № | Задача | Оценка |
|---|--------|--------|
| 9 | Добавить CSP для мобильного сервера и renderer | 2–4 ч |
| 10 | Заменить `@ts-ignore` и `any` на корректные типы | Постепенно |
| 11 | Провести audit неиспользуемого кода (lint:unused, knip) | 1–2 ч |

---

## Чек-лист для следующего аудита

- [ ] Path traversal исправлен и покрыт тестами
- [ ] Express limit настроен
- [ ] Rate limiting включён
- [ ] Проверка размера файлов реализована
- [ ] Дублирование useMatch устранено
- [ ] CSP настроен
- [ ] Запущен полный прогон `npm run lint:unused` и тестов безопасности
- [ ] Проверены обновления зависимостей на уязвимости (`npm audit`)

---

## Связанные документы

- [Инструкции по рефакторингу безопасности](security-refactoring-implementation-guide.md) — пошаговые инструкции для программистов
- [QA_AUDIT_REPORT.md](QA_AUDIT_REPORT.md) — общий аудит качества кода
- [XSS_FIX_SUMMARY.md](../troubleshooting/XSS_FIX_SUMMARY.md) — исправление XSS
- [vmix-connection-audit-report.md](../troubleshooting/vmix-connection-audit-report.md) — аудит vMix
- [REFACTORING_GUIDE.md](REFACTORING_GUIDE.md) — план рефакторинга
- [obfuscation-and-protection.md](obfuscation-and-protection.md) — защита от декомпиляции

---

*Последнее обновление: 2026-02-05*
