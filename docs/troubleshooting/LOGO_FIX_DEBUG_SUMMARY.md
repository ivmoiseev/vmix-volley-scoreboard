# Сводка по отладке проблемы с логотипами

**Дата:** 2024  
**Статус:** 🔍 Добавлено подробное логирование для отладки

---

## Проблема

После смены команд местами и последующего сохранения настроек матча:
- ✅ При смене команд местами логотипы правильно меняются местами в vMix
- ❌ При сохранении настроек логотипы в инпуте "Заявка" снова меняются местами в vMix
- ⚠️ Проблема только в инпуте "Заявка" (rosterTeamA/rosterTeamB)
- ✅ В других инпутах (составы команд, стартовые составы) логотипы работают правильно

---

## Добавленное логирование

### 1. HTTP API команды (`src/main/vmix-client.js`)

**Что логируется:**
- Все команды vMix API с полными деталями
- Для `SetImage` - полный URL логотипа
- Полный HTTP запрос для каждой команды

**Пример:**
```
[vMix API] SetImage → Input3
  Поле: TeamLogo
  Значение: http://192.168.1.100:3000/logos/logo_a.png
  [SetImage Logo] Полный URL: http://192.168.1.100:3000/logos/logo_a.png
  Полный HTTP запрос: http://localhost:8088/api/?Function=SetImage&Input=Input3&SelectedName=TeamLogo&Value=...
```

### 2. Формирование данных для инпутов (`src/renderer/hooks/useVMix.js`)

**В `formatRosterData`:**
- teamKey и inputKey при вызове
- Имя команды и logoPath
- Итоговые imageFields

**В `getRosterFieldValue`:**
- Формирование URL логотипа
- teamKey, logoFileName, сформированный URL
- Предупреждение при несоответствии logoPath

**В `updateRosterTeamAInput` и `updateRosterTeamBInput`:**
- Логотипы перед отправкой
- Проверка правильности имени файла

### 3. Сохранение настроек (`src/renderer/pages/MatchSettingsPage.jsx`)

**В `handleSave`:**
- Матч до и после `setCurrentMatch`
- Матч, используемый для обновления vMix

**В обработчике смены команд:**
- Матч до и после `swapTeams`
- Матч после `setCurrentMatch`
- Матч, используемый для обновления vMix

### 4. Обработка матча (`src/main/main.js`)

**В `match:set-current`:**
- Матч до и после обработки логотипов
- Обновление logoPath для каждой команды

---

## Как использовать логирование

### Шаги для воспроизведения проблемы:

1. Откройте консоль разработчика (F12) или терминал
2. Выполните действия:
   - Откройте матч с двумя командами и логотипами
   - Нажмите "Поменять команды местами"
   - Нажмите "Сохранить изменения" в настройках матча
3. Проверьте логи в консоли

### Что искать в логах:

1. **Правильность teamKey:**
   ```
   [formatRosterData] Вызов для teamKey=A, inputKey=rosterTeamA
   [getRosterFieldValue] teamLogo для teamKey=A:
     logoFileName (из teamKey): logo_a.png
   ```
   - Для `rosterTeamA` должен быть `teamKey=A` и `logoFileName=logo_a.png`
   - Для `rosterTeamB` должен быть `teamKey=B` и `logoFileName=logo_b.png`

2. **Правильность URL:**
   ```
   [getRosterFieldValue] Сформированный URL: http://192.168.1.100:3000/logos/logo_a.png
   ```
   - Для команды A должен быть URL с `logo_a.png`
   - Для команды B должен быть URL с `logo_b.png`

3. **Правильность матча:**
   ```
   [match:set-current] После обработки:
     teamA.name: Команда A, logoPath: logos/logo_a.png
     teamB.name: Команда B, logoPath: logos/logo_b.png
   ```
   - После `setCurrentMatch` logoPath должны быть обновлены
   - При обновлении vMix должен использоваться матч с правильными logoPath

4. **HTTP команды:**
   ```
   [vMix API] SetImage → Input3
     [SetImage Logo] Полный URL: http://192.168.1.100:3000/logos/logo_a.png
   ```
   - Проверьте, какие URL отправляются в vMix
   - Проверьте, соответствуют ли они правильным командам

---

## Возможные причины проблемы

### 1. Неправильный матч при сохранении

**Симптом:** В логах видно, что используется старый матч без обновленных logoPath

**Проверка:** Сравните матч до и после `setCurrentMatch` в логах

### 2. Неправильный teamKey

**Симптом:** В логах видно, что для `rosterTeamA` используется `teamKey=B`

**Проверка:** Проверьте логи `[formatRosterData]` и `[getRosterFieldValue]`

### 3. Двойное обновление

**Симптом:** В логах видно два вызова `updateRosterTeamAInput` или `updateRosterTeamBInput`

**Проверка:** Проверьте, не вызывается ли `updateMatchData` дважды

### 4. Кэширование

**Симптом:** В логах видно, что отправляются старые значения из кэша

**Проверка:** Проверьте, вызывается ли `resetImageFieldsCache()` перед обновлением

---

## Следующие шаги

1. **Воспроизвести проблему** с включенным логированием
2. **Собрать логи** из консоли
3. **Проанализировать логи** и найти причину
4. **Исправить проблему** на основе анализа логов
5. **Проверить исправление** с логированием
6. **Отключить избыточное логирование** после исправления

---

## Исправления, уже внесенные

1. ✅ Обновление `logoPath` в матче после `setCurrentMatch`
2. ✅ Возврат обновленного матча из `setCurrentMatch`
3. ✅ Использование обновленного матча при сохранении настроек
4. ✅ Использование обновленного матча при смене команд местами
5. ✅ Добавлено подробное логирование для отладки

---

**Дата создания:** 2024  
**Версия:** 1.0
