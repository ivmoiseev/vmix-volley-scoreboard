/**
 * Реэкспорт shared vmix-field-utils для использования в renderer.
 * Обеспечивает единый источник истины для полей vMix.
 */
export {
  FIELD_SUFFIXES,
  getFullFieldName,
  removeFieldSuffix,
  hasFieldSuffix,
  type FieldType,
} from '../../shared/vmix-field-utils';
